// variables5.rs
// Make me compile! Execute the command `rustlings hint variables5` if you want a hint :)

fn main() {
    let number = "3"; // don't change this line
    println!("Number {}", number);
    // number is a new variable that just happens to have the same name
    // it being a new variable means it can have a different type
    let number = 3;
    println!("Number {}", number);
}
